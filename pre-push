
# create file 1
# what is file 1? it is the artefact we already have in the repo
mv public/expense/index.md /tmp/index.md-backup

# create file 2
# what is file 2? it is the up to date artefact generated by hugo
hugo
mv public/expense/index.md /tmp/index.md-uptodate

# restore the original artefact to the same old location
cp /tmp/index.md-backup public/expense/index.md

# compare the two files and fail the push if they are different
diff /tmp/index.md-uptodate /tmp/index.md-backup